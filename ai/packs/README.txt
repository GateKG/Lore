Per-game sense packs.
<game>.txt      - sound prompts, one per line:   kind: what it sounds like
<game>.ocr.txt  - HUD patterns, one per line:    kind: regex
The game name is the folder name, lowercased, spaces removed
(e.g. rocketleague.txt). A pack REPLACES the default prompts.
